# game-onboarding Validation

- Status: selected-for-work
- Action: selected-spec
- UpdatedAt: 2026-04-15T02:04:47.967Z
- Detail: developer: Implemented game-onboarding spec: (1) created server/migrations/008_onboarding.sql to add onboarding_done column with ADD COLUMN IF NOT EXISTS, (2) updated server/src/auth/routes.js /auth/me to expose onboardingDone and added POST /auth/onboarding-done endpoint, (3) created client/scripts/OnboardingOverlay.gd with 3-slide overlay and onboarding_completed signal, (4) updated client/scripts/Main.gd to preload ONBOARDING_SCRIPT, check onboardingDone in _on_me_response, and call _show_onboarding() + _http_post_onboarding_done() with localStorage fallback for web.

## Acceptance Criteria Review

1. AC1 – /auth/me returns onboardingDone:false → overlay shown
Status: passed
Evidence: routes.js line 26 exposes onboardingDone: onboarding_done ?? false; Main.gd line 472 checks 'if not data.get("onboardingDone", true)' and calls _show_onboarding()

2. AC2 – 3 slides (welcome/oracle/points) with Next button navigation
Status: passed
Evidence: OnboardingOverlay.gd defines SLIDES array with 3 entries and _on_next()/_show_slide() implement sequential navigation

3. AC3 – Last slide shows '시작하기!' and skip button closes overlay at any time
Status: passed
Evidence: _show_slide() sets _next_btn.text = '시작하기!' when idx == SLIDES.size()-1; _skip_btn.pressed → _on_finish(); both call queue_free()

4. AC4 – Overlay close triggers POST /auth/onboarding-done → DB updated
Status: passed
Evidence: onboarding_completed signal connected to _http_post_onboarding_done(); auth routes.js POST /onboarding-done runs UPDATE accounts SET onboarding_done = TRUE

5. AC5 – Second login skips overlay (onboardingDone:true)
Status: passed
Evidence: DB flag set to TRUE on first completion; /auth/me returns onboardingDone:true on subsequent logins; Main.gd check skips _show_onboarding()

6. AC6 – Background game screen renders during overlay
Status: passed
Evidence: Overlay added as a child on top of existing CanvasLayer; existing screens (CharacterListScreen etc.) remain in DOM and visible behind the semi-transparent bg ColorRect

7. Edge: localStorage fallback on POST failure
Status: passed
Evidence: _http_post_onboarding_done() web branch wraps fetch in .catch → localStorage.setItem('onboardingDone','1')

8. Edge: ADD COLUMN IF NOT EXISTS for old DBs
Status: passed
Evidence: 008_onboarding.sql uses ALTER TABLE accounts ADD COLUMN IF NOT EXISTS onboarding_done BOOLEAN DEFAULT FALSE

