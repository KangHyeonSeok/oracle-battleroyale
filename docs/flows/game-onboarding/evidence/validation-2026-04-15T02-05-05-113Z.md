# game-onboarding Validation

- Status: validation-pending
- Action: advanced-spec
- UpdatedAt: 2026-04-15T02:05:05.113Z
- Detail: spec-validator: All 6 acceptance criteria and both edge cases verified by implementation evidence. Server migration, auth routes, OnboardingOverlay.gd, and Main.gd changes are all in place and aligned with spec requirements. Ready to advance to validating.

## Acceptance Criteria Review

1. AC1 – /auth/me returns onboardingDone:false → overlay shown
Status: passed
Evidence: routes.js exposes onboardingDone: onboarding_done ?? false; Main.gd checks 'if not data.get("onboardingDone", true)' and calls _show_onboarding()

2. AC2 – 3 slides (welcome/oracle/points) with Next button navigation
Status: passed
Evidence: OnboardingOverlay.gd defines SLIDES array with 3 entries; _on_next()/_show_slide() implement sequential navigation

3. AC3 – Last slide shows '시작하기!' and skip button closes overlay at any time
Status: passed
Evidence: _show_slide() sets next btn text to '시작하기!' on last slide; _skip_btn.pressed connects to _on_finish(); both paths call queue_free()

4. AC4 – Overlay close triggers POST /auth/onboarding-done → DB updated
Status: passed
Evidence: onboarding_completed signal connected to _http_post_onboarding_done(); auth routes.js POST /onboarding-done runs UPDATE accounts SET onboarding_done = TRUE

5. AC5 – Second login skips overlay (onboardingDone:true)
Status: passed
Evidence: DB flag set TRUE on first completion; /auth/me returns onboardingDone:true on subsequent logins; Main.gd check skips _show_onboarding()

6. AC6 – Background game screen renders during overlay
Status: passed
Evidence: Overlay added as child on top of existing CanvasLayer; existing screens remain in scene tree and visible behind semi-transparent ColorRect bg

7. Edge: localStorage fallback on POST failure
Status: passed
Evidence: _http_post_onboarding_done() web branch wraps fetch in .catch → localStorage.setItem('onboardingDone','1')

8. Edge: ADD COLUMN IF NOT EXISTS for old DBs
Status: passed
Evidence: 008_onboarding.sql uses ALTER TABLE accounts ADD COLUMN IF NOT EXISTS onboarding_done BOOLEAN DEFAULT FALSE

