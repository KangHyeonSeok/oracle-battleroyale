# oracle-ui-screens Validation

- Status: selected-for-work
- Action: selected-spec
- UpdatedAt: 2026-04-11T06:41:16.657Z
- Detail: developer: Implemented all 5 UI screens (CharacterList, CharacterCreate, MatchWait, OracleStream, GameResult) as complete GDScript files wired via Main.gd. Added missing WS message handlers on the server (get_characters, preview_character, create_character, find_match, cancel_match, oracle_send with room broadcast). Fixed matchmaker to send match_found (not match_started) to participants and added total field to queue_update. Added death tracking to GameState for proper rankings. Added session middleware export from app.js so WS connections can identify authenticated users.

## Acceptance Criteria Review

1. AC1: CharacterListScreen shows saved constellation cards and new-create button navigates to CharacterCreateScreen
Status: passed
Evidence: CharacterListScreen.gd fully implemented: _make_card() renders name/class/hp/atk/def/win_rate, create button emits create_character_requested, join button emits join_match_requested. Main.gd connects both signals and calls _show_screen('char_create') / _show_screen('match_wait'). Server now handles get_characters WS message and returns my_characters array.

2. AC2: CharacterCreateScreen calls Gemini via prompt and shows class/stats preview before saving
Status: passed
Evidence: CharacterCreateScreen.gd sends preview_character WS message on analyze press; server ws/server.js case 'preview_character' calls extractRulesTable(), maps class to base stats (warrior:HP150/ATK25/DEF15, archer:HP100/ATK20/DEF8, mage:HP80/ATK35/DEF5, etc.) with 0.8-1.3x multiplier, returns character_preview. Save button sends create_character which persists to DB with correct stats.

3. AC3: MatchWaitScreen enters queue, shows participant count and countdown with real-time updates; system auto-starts match
Status: passed
Evidence: MatchWaitingScreen.gd sends find_match WS; server enqueues via matchmaker.enqueue(). Matchmaker now broadcasts queue_update with {count, total:32, waitSeconds}. Client updates _count_lbl text in real-time. _process() increments elapsed time for countdown. After 60s timeout, matchmaker auto-starts match with NPC fill. match_found sent to participants (fixed from match_started).

4. AC4: OracleStreamPanel shows oracle message history chronologically; spectators and participants can send oracles
Status: passed
Evidence: OracleStreamPanel.gd adds messages to _msg_container in arrival order with auto-scroll. Handles oracle (sender/target/success badge), spectator, and system message types. WS handler oracle_send processes via Gemini intent extraction + credulity check + deductPoint + pushOverride, then broadcastToRoom with oracle_stream message. is_spectator flag passed through.

5. AC5: MatchResultScreen shows winner, rankings, points summary, and replay button works
Status: passed
Evidence: GameResultScreen.gd show_result() populates _winner_lbl, builds rank rows from rankings array (gold/silver/bronze coloring), shows points summary string. Main._on_game_ended now builds rankings from GameState.death_log (deaths tracked per turn_result event) reversed for placement order, winner first. play_again_requested and main_menu_requested signals wired back to char_list.

6. AC6: Full flow app-entry to result without interruption
Status: passed
Evidence: Main.gd _show_screen() controls visibility for all screens: char_list → char_create → char_list → match_wait → arena+oracle_stream → result → char_list. All transitions covered by signal handlers. WS-based auth via session middleware applied on connection; userId tracked as ws._userId and ws._currentMatchId set on join_match for oracle routing.

