# class-balance Validation

- Status: review-required
- Action: advanced-spec
- UpdatedAt: 2026-04-09T10:40:25.194Z
- Detail: spec-validator: Implementation is largely complete with 5/6 ACs passing. AC1 is partial: the warrior vs warrior simulation yields exactly 15 turns via the specified formula and stats (max(1,15*(10/10)-10*0.5)=10 dmg, 150HP/10=15 turns), which exceeds the spec-stated 10±3 (7–13) range. This is a contradiction between the formula/stats and the acceptance criterion — not a coding error. Human review is needed to decide whether to adjust AC1's tolerance, tune the warrior stats, or accept the current result.

## Acceptance Criteria Review

1. AC1: warrior vs warrior kill in 10±3 turns
Status: partial
Evidence: Formula produces exactly 15 turns (effective_dmg=10, 150HP/10=15). Exceeds 7–13 spec range. Formula and stats are self-consistent; the AC range appears misaligned with the finalized stats table.

2. AC2: warrior vs mage 10 rounds — mage wins 3+
Status: passed
Evidence: Orbital kiting simulation shows mage wins 10/10. Higher ATK (16) and ranged type dominate when movement is modeled.

3. AC3: assassin vs healer 10 rounds — assassin wins 7+
Status: passed
Evidence: Assassin wins 10/10. 25% crit (×2) burst overcomes healer's +20HP heal on 3-turn cooldown.

4. AC4: NPC presets 5 classes with max_hp included in INSERT
Status: passed
Evidence: npc-presets.js contains all 5 classes (warrior/archer/mage/assassin/healer) with corrected stats and max_hp field in ensureNpcCharactersExist INSERT.

5. AC5: hp_percent uses max_hp — warrior 112/150 → 75%
Status: passed
Evidence: buildCharacterGameState updated to use character.max_hp with fallback. Test confirms correct percentage.

6. AC6: migration 004 runs without error, max_hp column present
Status: passed
Evidence: 004_class_balance.sql uses ADD COLUMN IF NOT EXISTS, UPDATE max_hp=hp, SET NOT NULL, SET DEFAULT 100 — idempotent and verified.

