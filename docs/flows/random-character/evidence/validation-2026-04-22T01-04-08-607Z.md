# random-character Validation

- Status: selected-for-work
- Action: selected-spec
- UpdatedAt: 2026-04-22T01:04:08.607Z
- Detail: developer: Implemented random character generation: added `generateRandomCharacter()` to gemini.js with a Korean-fantasy Gemini prompt and fallback, wired `random_character` WS case in server.js that responds with `character_preview` including a `name` field, and updated CharacterCreateScreen.gd to add `_random_btn` in an HBoxContainer beside `_analyze_btn`, send `{type:random_character}` on click, show 'AI 생성 중...' loading state, and auto-fill `_name_input` + `_pending_name` from the server's returned name on `character_preview`.

## Acceptance Criteria Review

1. AC-1 버튼 노출: CharacterCreateScreen에 '🎲 랜덤 생성' 버튼이 'AI로 스탯 분석하기' 버튼 옆에 표시된다
Status: passed
Evidence: CharacterCreateScreen.gd:130-151 — HBoxContainer btn_row holds both _analyze_btn and _random_btn with text '🎲 랜덤 생성'

2. AC-2 WS 전송: 버튼 클릭 시 {type: 'random_character'} WebSocket 메시지를 서버로 전송한다
Status: passed
Evidence: CharacterCreateScreen.gd:252 — WebSocketClient.send({"type": "random_character"}) in _on_random_pressed()

3. AC-3 서버 응답: 서버가 random_character 메시지를 받으면 Gemini로 랜덤 성좌명+스탯을 생성하고 character_preview 형식으로 응답 (name 포함)
Status: passed
Evidence: server.js:207-225 — case 'random_character' calls generateRandomCharacter() then sends character_preview with name, class, hp, atk, def, tendency; gemini.js:87-108 — generateRandomCharacter() calls Gemini with Korean fantasy prompt

4. AC-4 자동 채워넣기: character_preview 수신 시 _name_input에 name 자동 입력, 미리보기 패널 표시
Status: passed
Evidence: CharacterCreateScreen.gd:289-300 — name_from_server extracted from preview_data, _name_input.text and _pending_name set if non-empty, _preview_panel.visible = true

5. AC-5 저장 가능: 랜덤 생성 후 '성좌 저장하기' 버튼 활성화, 정상 저장
Status: passed
Evidence: _save_btn.visible = true set in character_preview branch (line 301); _pending_name set from server name so create_character WS send uses it

6. AC-6 로딩 표시: 랜덤 생성 중 'AI 생성 중...' 레이블 표시, 버튼 비활성화
Status: passed
Evidence: CharacterCreateScreen.gd:245-251 — _random_btn.disabled=true, _analyze_btn.disabled=true, _loading_lbl.text='AI 생성 중...', _loading_lbl.visible=true

