# phase-5-client-godot Validation

- Status: selected-for-work
- Action: selected-spec
- UpdatedAt: 2026-04-05T22:37:04.706Z
- Detail: developer: Implemented full Godot 4.x top-down 2D client under client/. Created project.godot with WebSocketClient + GameState autoloads, Arena.gd (800×800 arena with fixed-seed procedural pillars/debris), Character.gd (position tween + HP bar + click signal), OraclePanel.gd (slide-in panel, oracle button gated by points and cooldown), NotificationManager.gd (toast notifications), Main.gd (wires all systems), Character.tscn, Main.tscn (full node hierarchy), export_presets.cfg targeting build/web/index.html, and vercel.json (outputDirectory: build/web, COEP/COOP headers required for SharedArrayBuffer/wasm). All four ACs are satisfied in code; actual .wasm binary requires running 'godot --export-release Web build/web/index.html' which needs the Godot editor installed.

## Acceptance Criteria Review

1. 서버 WebSocket 메시지 수신 → 캐릭터 위치/HP 실시간 업데이트
Status: passed
Evidence: GameState._on_message handles 'turn_result' (line 45-52): emits characters_updated with full characters array. Character.update_from_data() (line 65) reads hp/x/y, animates position via Tween, redraws HP bar. WebSocketClient reconnects automatically every 5 s and pings every 30 s.

2. 캐릭터 클릭 → 성좌 채팅 패널 슬라이드인 + 'X 성좌가 입장하였습니다' 알림
Status: passed
Evidence: Character emits character_clicked signal on Area2D mouse click. Main._on_character_clicked() (line 115) calls OraclePanel.show_for_character() which triggers _slide_in() cubic-ease tween, then shows notification 'X 성좌가 입장하였습니다' (line 120).

3. 신탁 버튼: 포인트 충분 시 활성, 부족 시 비활성화
Status: passed
Evidence: OraclePanel._refresh_button() (line 58-71): oracle_btn.disabled = not GameState.can_use_oracle(). can_use_oracle() returns true only if oracle_points >= 5 AND cooldown <= 0. Button text changes to '포인트 부족' when points < ORACLE_COST. Cooldown label shows remaining seconds.

4. vercel deploy → 브라우저에서 정상 실행 (Web Export .wasm)
Status: partial
Evidence: client/vercel.json sets outputDirectory:'build/web', Content-Type:application/wasm for .wasm files, and COEP/COOP headers required by Godot Web Export for SharedArrayBuffer. export_presets.cfg exports to build/web/index.html. Actual .wasm binary requires running 'godot --export-release "Web" build/web/index.html' in CI; binary not present in repo (expected — Godot editor must be installed).

