# phase-5-client-godot Validation

- Status: review-required
- Action: advanced-spec
- UpdatedAt: 2026-04-05T22:37:24.125Z
- Detail: spec-validator: Implementation is complete in code for all four ACs. Three ACs are fully satisfied; AC4 (vercel deploy → browser execution) is partial because the .wasm binary has not been built or deployed — the config and export preset are correct but no live URL exists to verify. Recommend needs-review so a human or CI step can run the Godot export and confirm browser execution.

## Acceptance Criteria Review

1. 서버 WebSocket 메시지 수신 → 캐릭터 위치/HP 실시간 업데이트
Status: passed
Evidence: GameState._on_message handles 'turn_result', emits characters_updated. Character.update_from_data() reads hp/x/y, animates via Tween, redraws HP bar. WebSocketClient auto-reconnects every 5s.

2. 캐릭터 클릭 → 성좌 채팅 패널 슬라이드인 + 'X 성좌가 입장하였습니다' 알림
Status: passed
Evidence: Character emits character_clicked via Area2D. Main._on_character_clicked() calls OraclePanel.show_for_character() → _slide_in() tween + notification toast 'X 성좌가 입장하였습니다'.

3. 신탁 버튼: 포인트 충분 시 활성, 부족 시 비활성화
Status: passed
Evidence: OraclePanel._refresh_button() gates oracle_btn.disabled on GameState.can_use_oracle() (points >= 5 AND cooldown <= 0). Button label changes to '포인트 부족' when insufficient; cooldown seconds shown.

4. vercel deploy → 브라우저에서 정상 실행 (Web Export .wasm)
Status: partial
Evidence: vercel.json and export_presets.cfg are correctly configured with COEP/COOP headers and wasm Content-Type. However, .wasm binary has not been built ('godot --export-release' not run) and no live Vercel URL is available to confirm browser execution.

