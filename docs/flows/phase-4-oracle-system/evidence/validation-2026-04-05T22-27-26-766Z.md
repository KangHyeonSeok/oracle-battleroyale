# phase-4-oracle-system Validation

- Status: selected-for-work
- Action: selected-spec
- UpdatedAt: 2026-04-05T22:27:26.766Z
- Detail: developer: Implemented Phase 4 oracle system: Gemini OracleIntent extraction, credulity check, Redis override_queue, constellation points pipeline, HTTP POST /oracle route with cooldown and point guards, turn-scheduler integration for override consumption and survival/win/reversal point awards.

## Acceptance Criteria Review

1. 신탁 메시지 → Gemini 분류 → 유형(강화/방해/유혹) + 대상 추출
Status: passed
Evidence: server/src/oracle/intent.js: extractOracleIntent() calls Gemini with a structured prompt that classifies buff/debuff/lure and returns actionOverride. Keyword fallback included for environments without GEMINI_API_KEY.

2. credulity 판정: 성공 시 override_queue에 삽입, 다음 턴 AI 행동에 반영
Status: passed
Evidence: oracle/credulity.js: buff always succeeds; debuff/lure rolls Math.random()*100 < credulity. On success routes.js calls pushOverride() into Redis LIST game:match:{id}:override_queue. turn-scheduler.js calls popCharacterOverrides() per character before evaluateAction(), applying override instead of AI decision.

3. 쿨다운: 동일 플레이어 분당 1회 초과 시 거부 응답
Status: passed
Evidence: oracle/routes.js lines 87-93: Redis key oracle:cooldown:{userId}:{matchId} with EX=60s checked before processing; returns HTTP 429 with remaining TTL if set.

4. 포인트 부족 시 신탁 거부, 포인트 차감 정확성 검증
Status: passed
Evidence: routes.js: fast check req.user.constellation_points < 5 returns 402. oracle/points.js deductOracleCost() uses atomic UPDATE ... WHERE constellation_points >= 5 RETURNING, throwing if no row updated. Survival +1/turn, win +5, dramatic reversal +15 (5+10) in awardSurvivalPoints/awardWinPoints called from turn-scheduler.

5. DB migration: credulity column added to characters
Status: passed
Evidence: server/migrations/003_oracle_credulity.sql: ALTER TABLE characters ADD COLUMN IF NOT EXISTS credulity INTEGER NOT NULL DEFAULT 50. match-routes.js participant query updated to include c.credulity; redis-state.js buildInitialState carries it into character state.

6. Node.js syntax validation
Status: passed
Evidence: node --check on all 5 new oracle files + turn-scheduler.js + app.js all returned exit 0 with 'ALL OK'.

